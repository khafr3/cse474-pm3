apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-index-service
  namespace: cse474-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: search-index-service
  template:
    metadata:
      labels:
        app: search-index-service
    spec:
      containers:
        - name: search-index-service
          image: YOUR_DOCKERHUB_USERNAME/search-index-service:latest
          ports:
            - containerPort: 4001
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          envFrom:
            - configMapRef:
                name: search-index-config
            - secretRef:
                name: search-index-secret
          livenessProbe:
            httpGet:
              path: /api/health
              port: 4001
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/ready
              port: 4001
            initialDelaySeconds: 5
            periodSeconds: 5
